name: adds comments on issues

on:
  issues:
    types: [opened]
    
jobs: 
    comment-with-action:
        runs-on: ubuntu-latest
        steps:
        - name: dump context
          Run: echo ‘${{ to Json(github.event) }}’ | jq
          Shell: bash
